import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "m.media-amazon.com", "tm.ibxk.com.br", "www.cnnbrasil.com.br"],
  },
};

export default nextConfig;
